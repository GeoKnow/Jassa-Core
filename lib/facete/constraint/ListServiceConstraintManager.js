/*
var ListServiceConstraintManager = Class.create({
    initialize: function(constraintManager) {
        this.constraintManager = constraintMangaer;

        this.filterSupplierFn = function(searchString) {
            return function(constraint) {

            }
        };
    },

    fetchItems: function(filter, limit, offset) {
        var ls = new ListServiceArray(constraints, fnFilterSupplier);
        return
    },

    fetchCount: function(filter, itemLimit, rowLimit) {

    }
});

module.exports = ListServiceConstaintManager;
*/